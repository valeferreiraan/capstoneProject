import './Subheader.css';

function Subheader({title}){
    return(
        <div class="subheader">
            <div class="Title">
                <h1>{title}</h1>
            </div>
        </div>
    )
}

export default Subheader;